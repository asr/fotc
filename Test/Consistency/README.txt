Test the consistency of the axioms

In some modules, we declare Agda postulates as FOL axioms. In this
directory we test in these modules if it is possible to prove an
unprovable theorem from these axioms (see the rule all_consistency in
src/Makefile).
