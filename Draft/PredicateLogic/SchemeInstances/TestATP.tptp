%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Testing the use of schema
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% The Agda version of the scheme
% scheme : (P Q : D → Set){x : D} → P x → Q x

% Koen's translation
fof(scheme, axiom, ( ! [P,Q,X] : (app(P,X) => app(Q,X)))).

% Scheme instance
fof(instance, conjecture, app(p,x) => app(p,x)).
